import React, { useState } from 'react';

import {
  StyledContainer
} from './styles';

const Chat = ({ navigation }) => {

  return (
    <StyledContainer>
    </StyledContainer>
  );
};

export default Chat;
