import React, { useState } from 'react';

import {
  StyledContainer
} from './styles';

const Home = ({ navigation }) => {

  return (
    <StyledContainer>
    </StyledContainer>
  );
};

export default Home;
