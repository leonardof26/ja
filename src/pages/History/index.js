import React, { useState } from 'react';

import {
  StyledContainer
} from './styles';

const History = ({ navigation }) => {

  return (
    <StyledContainer>
    </StyledContainer>
  );
};

export default History;
